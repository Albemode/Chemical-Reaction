<%= form_for(pain) do |f| %>
  <% if pain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pain.errors.count, "error") %> prohibited this pain from being saved:</h2>

      <ul>
      <% pain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
