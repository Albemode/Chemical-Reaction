<%= form_for(saddness) do |f| %>
  <% if saddness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saddness.errors.count, "error") %> prohibited this saddness from being saved:</h2>

      <ul>
      <% saddness.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
