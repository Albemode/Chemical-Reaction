<%= form_for(nitrou) do |f| %>
  <% if nitrou.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nitrou.errors.count, "error") %> prohibited this nitrou from being saved:</h2>

      <ul>
      <% nitrou.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
