<%= form_for(patience) do |f| %>
  <% if patience.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patience.errors.count, "error") %> prohibited this patience from being saved:</h2>

      <ul>
      <% patience.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
