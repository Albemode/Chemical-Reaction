<%= form_for(codeine) do |f| %>
  <% if codeine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(codeine.errors.count, "error") %> prohibited this codeine from being saved:</h2>

      <ul>
      <% codeine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
