<%= form_for(mx) do |f| %>
  <% if mx.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mx.errors.count, "error") %> prohibited this mx from being saved:</h2>

      <ul>
      <% mx.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
