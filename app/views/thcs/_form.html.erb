<%= form_for(thc) do |f| %>
  <% if thc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thc.errors.count, "error") %> prohibited this thc from being saved:</h2>

      <ul>
      <% thc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
