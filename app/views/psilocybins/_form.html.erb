<%= form_for(psilocybin) do |f| %>
  <% if psilocybin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(psilocybin.errors.count, "error") %> prohibited this psilocybin from being saved:</h2>

      <ul>
      <% psilocybin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
