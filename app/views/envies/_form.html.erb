<%= form_for(envy) do |f| %>
  <% if envy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(envy.errors.count, "error") %> prohibited this envy from being saved:</h2>

      <ul>
      <% envy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
