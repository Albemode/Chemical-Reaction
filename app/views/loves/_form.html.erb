<%= form_for(lofe) do |f| %>
  <% if lofe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lofe.errors.count, "error") %> prohibited this lofe from being saved:</h2>

      <ul>
      <% lofe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
