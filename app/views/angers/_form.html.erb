<%= form_for(anger) do |f| %>
  <% if anger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(anger.errors.count, "error") %> prohibited this anger from being saved:</h2>

      <ul>
      <% anger.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
