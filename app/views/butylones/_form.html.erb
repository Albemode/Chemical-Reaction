<%= form_for(butylone) do |f| %>
  <% if butylone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(butylone.errors.count, "error") %> prohibited this butylone from being saved:</h2>

      <ul>
      <% butylone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
