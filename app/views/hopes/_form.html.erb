<%= form_for(hope) do |f| %>
  <% if hope.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hope.errors.count, "error") %> prohibited this hope from being saved:</h2>

      <ul>
      <% hope.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
