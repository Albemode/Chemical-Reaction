<script src="https://api.fda.gov/drug/event.json?api_key=xoGgcwZHxyuCoySIaLq1ZYuhQ8TSJ1Uu5doHLbnv">

</script>
<body class="secondpage"></body>
<p id="notice"><%= notice %></p>

<h1>List of Drugs</h1>

<nav>
  <li><%= link_to "Butylone", butylones_path %></li>
  <li><%= link_to "Alcohol", alcohols_path %></li>
  <li><%= link_to "Ether", ethers_path %></li>
</nav>

<table id="linkD">
  <thead>
    <tr>
      <th colspan="1">Brand Name</th>
      <th colspan="1">Description</th>
      <th colspan="1">Control</th>
      <th colspan="1">Report</th>
      <th colspan="1">Records</th>
      <th colspan="1">Event</th>
      <th colspan="1">Effects</th>
      <th colspan="1">Administration</th>

    </tr>
  </thead>

  <tbody id="linkD">
    <% @drugs.each do |drug| %>
      <tr>
        <td><%= drug.brand_name %></td>
        <td><%= drug.description %></td>
        <td><%= drug.control %></td>
        <td><%= drug.report %></td>
        <td><%= drug.records %></td>
        <td><%= drug.event %></td>
        <td><%= drug.effects %></td>
        <td><%= drug.administration %></td>
        <td><%= link_to 'Show', drug %></td>
        <td><%= link_to 'Edit', edit_drug_path(drug) %></td>
        <td><%#= link_to 'Destroy', drug, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Drug', new_drug_path %>

<div class="ctinfo1">

</div>

<div class="ctinfo2">

</div>

<div id="container" style="min-width: 310px; height: 400px; margin: 0 auto"></div>


<script type="text/javascript">
$(function () {
  $.getJSON('https://www.highcharts.com/samples/data/jsonp.php?filename=usdeur.json&callback=?', function (data) {

      Highcharts.chart('container', {
          chart: {
              zoomType: 'x'
          },
          title: {
              text: 'Duration rate'
          },
          subtitle: {
              text: document.ontouchstart === undefined ?
                      'Click and drag in the plot area to zoom in' : 'Pinch the chart to zoom in'
          },
          xAxis: {
              type: 'Timer'
          },
          yAxis: {
              title: {
                  text: 'Dose Level'
              }
          },
          legend: {
              enabled: false
          },
          plotOptions: {
              area: {
                  fillColor: {
                      linearGradient: {
                          x1: 1,
                          y1: 2,
                          x2: 1,
                          y2: 1
                      },
                      stops: [
                          [0, Highcharts.getOptions().colors[0]],
                          [1, Highcharts.Color(Highcharts.getOptions().colors[0]).setOpacity(0).get('rgba')]
                      ]
                  },
                  marker: {
                      radius: 5
                  },
                  lineWidth: 1,
                  states: {
                      hover: {
                          lineWidth: 1
                      }
                  },
                  threshold: null
              }
          },

          series: [{
              type: 'area',
              name: 'Low to High',
              data: data
          }]
      });
  });
});
</script>
